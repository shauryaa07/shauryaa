# Database Configuration
# Required: PostgreSQL database connection string
# For Neon: postgresql://user:password@host/database
# For local: postgresql://localhost:5432/myapp
DATABASE_URL=postgresql://user:password@host/database

# Optional: Set to "false" to disable SSL for local development
# Default: SSL is enabled with rejectUnauthorized=false
DATABASE_SSL=true

# Optional: Force use of Neon HTTP driver (serverless mode)
# Default: Auto-detected (uses Neon HTTP on Replit, node-postgres elsewhere)
# USE_NEON_HTTP=true

# Firebase Configuration (if using Firebase)
# Get these from your Firebase project settings
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Session Secret (for production, use a long random string)
SESSION_SECRET=change-this-to-a-random-secret-in-production
